---
"mucu": minor
---

Add automatic path alias detection from tsconfig.json/jsconfig.json. The CLI now automatically detects existing path aliases from your TypeScript/JavaScript configuration and uses them during component initialization, with an interactive prompt to customize if needed.